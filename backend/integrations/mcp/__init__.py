"""
MCP Integration
===============

Model Context Protocol (MCP) server exposing kanban operations to AI agents.

Usage:
    python -m integrations.mcp.server          # stdio (local)
    python -m integrations.mcp.server --http   # HTTP (remote)
"""
